export default
{
	/**
	 * Integer, in milliseconds
	 *
	 * How quickly the game loops through Ticker objects and applies updates.
	 */
	'tick_interval': 1000,

	/**
	 * Integer, in milliseconds
	 *
	 * How quickly the game redraws resource counts and progress bars; it never
	 * needs to be faster than the tick_interval.
	 */
	'animation_interval': 1000,
}